# Server
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

# Firebase Admin (optional until Firestore is connected)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Dev mode: bypass Firebase token verification (development only)
AUTH_DEV_MODE=true
